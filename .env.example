# ==================== Application ====================
APP_NAME="NursePrompt API"
APP_ENV=production
APP_PORT=8050

# ==================== Database (PostgreSQL) ====================
# Sesuaikan dengan container PostgreSQL yang sudah running di VPS
DB_HOST=secure_postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_password
DB_NAME=boilerplate
DB_SSL_MODE=disable

# ==================== Redis ====================
# Sesuaikan dengan container Redis yang sudah running di VPS
REDIS_HOST=redis-auth
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ==================== JWT ====================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY=24h
JWT_REFRESH_EXPIRY=168h

# ==================== Rate Limiting ====================
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=1m

# ==================== Security ====================
BCRYPT_COST=12

# ==================== Docker Network ====================
# Gunakan network yang sama dengan container database/redis
# Cek dengan: docker network ls
# Atau gunakan 'bridge' sebagai default
DOCKER_NETWORK=bridge